# 🚀 快速开始指南

## 第一步：编译项目

在项目目录中打开终端，运行：

```bash
cargo build --release
```

首次编译会下载依赖，可能需要几分钟时间。

## 第二步：运行程序

编译完成后，运行：

```bash
cargo run
```

或者直接运行可执行文件：

```bash
./target/release/timer-assistant.exe
```

## 第三步：使用程序

### 添加定时任务

1. **设置时间**：拖动"小时"和"分钟"滑块选择时间
2. **选择任务类型**：
   - 🔴 关机 - 关闭计算机
   - 🔄 重启 - 重启计算机
   - 🔒 锁屏 - 锁定屏幕
   - 💬 弹窗提醒 - 显示提醒消息
3. **点击"添加定时任务"**按钮

### 管理任务

- **启用/禁用**：点击任务右侧的 ✅/⏸️ 按钮
- **删除任务**：点击任务右侧的 ❌ 按钮
- **清空所有**：点击底部的"清空所有任务"按钮

### 托盘运行

- 点击"最小化到托盘"让程序在后台运行
- 点击系统托盘图标可以恢复窗口

## ⚠️ 重要提示

### 权限问题

关机和重启需要管理员权限。如果遇到权限错误：

**方法一：以管理员身份运行**
```powershell
# 右键点击程序，选择"以管理员身份运行"
# 或在 PowerShell 中运行：
Start-Process .\target\release\timer-assistant.exe -Verb RunAs
```

**方法二：嵌入 manifest（高级）**
项目已包含 `timer-assistant.exe.manifest` 文件，可以在编译时嵌入。

### 测试建议

首次使用时，建议先测试"弹窗提醒"功能，确认程序正常工作后再使用关机等高风险功能。

## 🐛 常见问题

### 编译失败

确保已安装 Visual Studio Build Tools：
```bash
# 访问 https://visualstudio.microsoft.com/zh-hans/downloads/
# 下载并安装 "Visual Studio Build Tools"
# 选择 "C++ build tools" 工作负载
```

### 托盘图标不显示

这是正常的，程序会在系统托盘区域显示图标。如果看不到，检查系统托盘的隐藏图标区域。

### 任务没有执行

1. 确认任务状态是启用（✅）
2. 确认系统时间正确
3. 查看控制台输出日志

## 📞 需要帮助？

如果遇到问题，请检查：
1. 控制台输出的错误信息
2. Windows 事件查看器中的应用程序日志
3. README.md 中的详细说明

祝你使用愉快！🎉
